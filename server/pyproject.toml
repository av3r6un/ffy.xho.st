[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.2",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "yt-dlp[default]>=2026.7.4",
]
